# Archive

This folder holds retired matcher experiments and historical implementation variants.

- `legacy_core/`: older core matcher scripts
- `legacy_gui/`: older GUI variants
- `legacy_colab/`: Colab-era helpers

The active application now lives under `src/curriculum_matcher/`.
